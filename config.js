/* BITE MORE — RUNTIME CONFIG
   GA4 is deployed THROUGH Google Tag Manager (container GTM-PT8568DB).
   There is deliberately no gtag.js here — a second GA4 load would double
   every page_view and every conversion. */
window.BM_CONFIG = {
  GTM_CONTAINER_ID:   "GTM-PT8568DB",   // installed in <head> on every page
  GA4_MEASUREMENT_ID: "G-220KLBJVG4",   // configured inside GTM, NOT loaded here
  META_PIXEL_ID:      "",               // NOT PROVIDED — do not invent
  ORDER_URL: "https://order.online/business/bite-more-13060526"
};
